<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Historia clínica de emergencia | Dr. Santiago Saralegui</title>
  <meta name="description" content="Herramienta web para crear historias clínicas de emergencia de forma rápida, ordenada y lista para copiar. Desarrollada por Dr. Santiago Saralegui.">
  <meta name="robots" content="index, follow">
  <meta name="author" content="Dr. Santiago Saralegui">
  <meta property="og:title" content="Historia clínica de emergencia">
  <meta property="og:description" content="Generador rápido de historias clínicas de emergencia para profesionales de la salud.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="og-image.png">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="icon" href="favicon-64.png" type="image/png">
  <link rel="apple-touch-icon" href="apple-touch-icon.png">
  <link rel="manifest" href="site.webmanifest">
  <script>
    const GA_MEASUREMENT_ID = "G-SN3JTPR3M0";
    if (GA_MEASUREMENT_ID) {
      const gaScript = document.createElement("script");
      gaScript.async = true;
      gaScript.src = `https://www.googletagmanager.com/gtag/js?id=${GA_MEASUREMENT_ID}`;
      document.head.appendChild(gaScript);
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag("js", new Date());
      gtag("config", GA_MEASUREMENT_ID);
    }
  </script>
  <style>
    :root {
      --bg: #f4f6f8;
      --panel: #ffffff;
      --ink: #17202a;
      --muted: #5d6978;
      --line: #d8dee6;
      --accent: #1d6f8f;
      --accent-dark: #14546e;
      --ok: #1f7a4d;
      --warn: #9a5a00;
      --field: #fbfcfd;
      --shadow: 0 10px 28px rgba(15, 23, 42, 0.08);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    * { box-sizing: border-box; }

    [hidden] { display: none !important; }

    body {
      margin: 0;
      background: var(--bg);
      color: var(--ink);
      min-height: 100vh;
    }

    header {
      min-height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 16px clamp(16px, 3vw, 32px);
      background: #ffffff;
      border-bottom: 1px solid var(--line);
      position: sticky;
      top: 0;
      z-index: 20;
    }

    h1 {
      margin: 0;
      font-size: clamp(20px, 2vw, 28px);
      letter-spacing: 0;
      line-height: 1.15;
    }

    .header-actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .top-menu {
      display: flex;
      gap: 6px;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      flex: 1 1 auto;
    }

    .menu-item {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 1px solid var(--line);
      background: #f7f9fb;
      color: #27313d;
      border-radius: 6px;
      min-height: 36px;
      padding: 0 10px;
      font-weight: 700;
      cursor: pointer;
      white-space: nowrap;
      text-decoration: none;
    }

    .menu-item:hover {
      border-color: var(--accent);
      background: #eef5f7;
    }

    main {
      display: grid;
      grid-template-columns: minmax(360px, 0.95fr) minmax(360px, 1.05fr);
      gap: 18px;
      padding: 18px clamp(12px, 3vw, 32px) 28px;
      max-width: 1680px;
      margin: 0 auto;
    }

    .column {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 8px;
      box-shadow: var(--shadow);
      min-width: 0;
    }

    .form-column {
      padding: 16px;
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    section {
      border-top: 1px solid var(--line);
      padding-top: 16px;
    }

    section:first-child {
      border-top: 0;
      padding-top: 0;
    }

    h2 {
      font-size: 15px;
      line-height: 1.2;
      margin: 0 0 12px;
      text-transform: uppercase;
      color: #27313d;
      letter-spacing: 0.06em;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .grid.three {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    label {
      display: flex;
      flex-direction: column;
      gap: 5px;
      font-size: 13px;
      color: var(--muted);
      min-width: 0;
    }

    input, textarea, select {
      width: 100%;
      border: 1px solid var(--line);
      background: var(--field);
      color: var(--ink);
      border-radius: 6px;
      padding: 9px 10px;
      font: inherit;
      min-height: 38px;
      outline: none;
    }

    textarea {
      min-height: 96px;
      resize: vertical;
      line-height: 1.35;
    }

    input:focus, textarea:focus, select:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(29, 111, 143, 0.14);
      background: #ffffff;
    }

    .expanded-editor {
      position: fixed;
      z-index: 100;
      width: min(520px, calc(100vw - 24px));
      min-height: 120px;
      resize: both;
      box-shadow: 0 12px 32px rgba(15, 23, 42, 0.2);
    }

    .ecg-options {
      display: grid;
      grid-template-columns: auto auto minmax(0, 1fr);
      gap: 10px;
      align-items: center;
      margin-top: 12px;
      padding: 10px 12px;
      border: 1px solid var(--line);
      border-radius: 6px;
      background: #f7f9fb;
    }

    .ecg-options.is-disabled {
      background: #eef1f4;
      color: #7a8490;
    }

    .ecg-editor-label {
      display: block;
      grid-column: 1 / -1;
      min-width: 0;
    }

    .ecg-editor-label > span {
      display: block;
    }

    .ecg-normal-editor {
      display: block;
      width: 100%;
      min-height: 40px;
      padding: 5px 10px;
      border: 1px solid var(--line);
      border-radius: 6px;
      background: #eef1f4;
      color: #697482;
      line-height: 1.45;
      white-space: normal;
    }

    .ecg-normal-editor input {
      display: inline-block;
      width: 52px;
      min-height: 30px;
      margin: 0 4px;
      padding: 3px 6px;
      text-align: center;
      background: #ffffff;
      color: var(--ink);
    }

    .ecg-custom-editor {
      min-height: 40px;
      height: 40px;
      resize: vertical;
    }

    .check-row {
      display: flex;
      gap: 12px;
      align-items: center;
      flex-wrap: wrap;
      margin-top: 10px;
    }

    .check {
      flex-direction: row;
      align-items: flex-start;
      color: #27313d;
      width: auto;
      max-width: 100%;
      gap: 8px;
      min-height: 32px;
    }

    .check input {
      width: 18px;
      height: 18px;
      min-height: 18px;
      accent-color: var(--accent);
    }

    .field-with-check {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 8px;
      align-items: end;
      min-width: 0;
    }

    .field-with-check .check {
      min-height: 38px;
      align-items: center;
      padding: 0 8px;
      border: 1px solid var(--line);
      border-radius: 6px;
      background: #f7f9fb;
      white-space: nowrap;
    }

    .antecedente-row {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) minmax(0, 1fr) 38px;
      gap: 8px;
      align-items: end;
      margin-bottom: 8px;
    }

    .mini-btn {
      width: 38px;
      height: 38px;
      border: 1px solid var(--line);
      background: #f7f9fb;
      color: #344052;
      border-radius: 6px;
      cursor: pointer;
      font-size: 20px;
      line-height: 1;
    }

    .mini-btn:hover { background: #eef3f6; }

    .button {
      border: 0;
      background: var(--accent);
      color: #ffffff;
      border-radius: 6px;
      min-height: 40px;
      padding: 0 14px;
      font-weight: 700;
      cursor: pointer;
      white-space: nowrap;
    }

    .button:hover { background: var(--accent-dark); }

    .button.secondary {
      background: #e8edf1;
      color: #243040;
    }

    .button.secondary:hover { background: #dce4ea; }

    .button.warn {
      background: #f1e4cf;
      color: #714300;
    }

    .button.warn:hover { background: #e7d4b5; }

    .preview-column {
      display: grid;
      grid-template-rows: auto 1fr;
      min-height: calc(100vh - 126px);
      overflow: hidden;
    }

    .preview-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      padding: 14px 16px;
      border-bottom: 1px solid var(--line);
      background: #fbfcfd;
    }

    .status {
      color: var(--muted);
      font-size: 13px;
      min-height: 18px;
    }

    #historia {
      margin: 0;
      padding: 18px;
      overflow: auto;
      white-space: pre-wrap;
      line-height: 1.45;
      font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
      font-size: 14px;
      background: #ffffff;
    }

    .hint {
      color: var(--muted);
      font-size: 12px;
      margin: 7px 0 0;
    }

    .toast {
      color: var(--ok);
      font-weight: 700;
    }

    .suggestion-strip {
      max-width: 1680px;
      margin: 0 auto 24px;
      padding: 0 clamp(12px, 3vw, 32px);
      display: flex;
      justify-content: center;
    }

    .suggestion-link {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 40px;
      padding: 0 14px;
      border-radius: 6px;
      border: 1px solid var(--line);
      background: #ffffff;
      color: var(--accent-dark);
      font-weight: 700;
      text-decoration: none;
      box-shadow: var(--shadow);
    }

    .suggestion-link:hover {
      border-color: var(--accent);
      background: #f7fbfc;
    }

    footer {
      border-top: 1px solid var(--line);
      background: #ffffff;
      color: var(--muted);
      padding: 18px clamp(16px, 3vw, 32px);
      text-align: center;
      font-size: 13px;
    }

    footer a {
      color: var(--accent-dark);
      font-weight: 700;
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 900px) {
      header {
        align-items: flex-start;
        flex-direction: column;
      }

      .header-actions {
        width: 100%;
        justify-content: stretch;
      }

      .top-menu {
        width: 100%;
        justify-content: flex-start;
      }

      .header-actions .button {
        flex: 1 1 130px;
      }

      main {
        grid-template-columns: 1fr;
      }

      .preview-column {
        min-height: 70vh;
      }
    }

    @media (max-width: 620px) {
      header {
        position: static;
      }

      .grid, .grid.three, .antecedente-row {
        grid-template-columns: 1fr;
      }

      .antecedente-row .mini-btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Historia clínica de emergencia</h1>
    <nav class="top-menu" aria-label="Opciones">
      <a class="menu-item" href="contacto.html">Contacto profesional</a>
    </nav>
    <div class="header-actions">
      <button class="button" id="copyTop" type="button">Copiar historia</button>
      <button class="button secondary" id="downloadTxt" type="button">Descargar TXT</button>
      <button class="button warn" id="clearAll" type="button">Limpiar</button>
    </div>
  </header>

  <main>
    <form class="column form-column" id="hcForm" autocomplete="off">
      <section>
        <h2>Ficha patronímica</h2>
        <div class="check-row">
          <label class="check"><input type="radio" name="sexo" data-field="sexoF" value="F"> Sexo femenino</label>
          <label class="check"><input type="radio" name="sexo" data-field="sexoM" value="M"> Sexo masculino</label>
        </div>
        <div class="grid three">
          <label>Edad
            <input data-field="edad" placeholder="Ej: 76">
          </label>
          <label>Trabajo
            <input data-field="trabajo" placeholder="Ej: jubilado, administrativo">
          </label>
          <label>Procedencia
            <input data-field="procedencia" placeholder="Ej: mutualista, emergencia móvil">
          </label>
        </div>
        <div class="grid">
          <label>Acompañante
            <input data-field="acompanante" placeholder="Ej: hija, solo">
          </label>
        </div>
        <div class="check-row">
          <label class="check"><input type="radio" name="funcionalidad" data-field="funcionalidadNoConsignar" checked> No consignar</label>
          <label class="check"><input type="radio" name="funcionalidad" data-field="autovalido"> Autoválido</label>
          <label class="check"><input type="radio" name="funcionalidad" data-field="dependiente"> Dependiente</label>
          <label class="check"><input type="checkbox" data-field="institucionalizado"> Institucionalizado</label>
        </div>
      </section>

      <section>
        <h2>Antecedentes personales</h2>
        <div id="antecedentes"></div>
        <button class="button secondary" id="addAntecedente" type="button">Agregar antecedente</button>
        <p class="hint">Tab pasa al tratamiento. Enter agrega otra línea.</p>
      </section>

      <section>
        <h2>Alergias</h2>
        <label class="check"><input type="checkbox" data-field="sinAlergias" checked> Sin alergias</label>
        <label id="alergiasLabel">Alergias
          <input data-field="alergias" placeholder="Ej: dipirona, penicilina">
        </label>
      </section>

      <section>
        <h2>Enfermedad actual</h2>
        <label>Relato
          <textarea data-field="enfermedad" placeholder="Consulta, síntomas, evolución, medicación recibida, etc."></textarea>
        </label>
        <div class="check-row">
          <label class="check"><input type="checkbox" data-field="noFiebreChuchos" checked> No fiebre, no chuchos de frío</label>
          <label class="check"><input type="checkbox" data-field="transitoNormal" checked> Tránsito sin alteraciones</label>
          <label class="check"><input type="checkbox" data-field="cardioNegativo"> No dolor torácico, palpitaciones, síncope ni mareos</label>
          <label class="check"><input type="checkbox" data-field="respNegativo"> No disnea ni dolor tipo puntada de lado</label>
        </div>
      </section>

      <section>
        <h2>Examen físico</h2>
        <div class="grid three">
          <label>PA
            <input data-field="pa" placeholder="Ej: 130/80">
          </label>
          <label>FC
            <input data-field="fc" value="80">
          </label>
          <label>FR
            <input data-field="fr" placeholder="Ej: 18">
          </label>
          <label>SatO2
            <input data-field="sat" placeholder="Ej: 98% VEA">
          </label>
          <label>Temperatura
            <input data-field="temp" placeholder="Ej: 36,8 °C">
          </label>
          <label>HGT
            <input data-field="hgt" placeholder="Ej: 112 mg/dL">
          </label>
        </div>
        <label>General
          <input data-field="general" value="Lúcido">
        </label>
        <label>Piel y mucosas
          <input data-field="pym" value="BHyP">
        </label>
        <div class="grid">
          <label>CV
            <input data-field="cv" value="RR, RBG, SL">
          </label>
          <label>MMII
            <input data-field="mmii" placeholder="Ej: sin edemas en MMII">
          </label>
        </div>
        <label>PP
          <input data-field="pp" value="MAV + bilateral, sin estertores">
        </label>
        <div class="grid">
          <div class="field-with-check">
            <label>ABD
              <input data-field="abd" placeholder="Ej: blando, depresible, indoloro, RHA +">
            </label>
            <label class="check"><input type="checkbox" data-field="abdNormal"> Normal</label>
          </div>
          <div class="field-with-check">
            <label>LG
              <input data-field="lg" placeholder="Ej: sin adenomegalias">
            </label>
            <label class="check"><input type="checkbox" data-field="lgNormal"> Normal</label>
          </div>
          <label>OA
            <input data-field="oa" placeholder="Ej: sin elementos fluxivos articulares">
          </label>
          <div class="field-with-check">
            <label>PNM
              <input data-field="pnm" placeholder="Ej: sin focalidad neurológica">
            </label>
            <label class="check"><input type="checkbox" data-field="pnmNormal"> Normal</label>
          </div>
          <div class="field-with-check">
            <label>FFLL
              <input data-field="ffll" placeholder="Ej: libres e indoloras">
            </label>
            <label class="check"><input type="checkbox" data-field="ffllNormal"> Normal</label>
          </div>
          <label>Otros EF
            <input data-field="otrosEf" placeholder="Otro hallazgo del examen físico">
          </label>
        </div>

        <div class="ecg-options is-disabled" id="ecgOptions">
          <label class="check"><input type="checkbox" data-field="incluirEcg"> ECG</label>
          <label class="check"><input type="checkbox" data-field="ecgNormal" checked disabled> Normal</label>
          <label class="ecg-editor-label" id="ecgNormalEditorLabel">
            <span class="ecg-normal-editor" aria-label="Descripción normal del ECG; solo la frecuencia cardíaca es editable">
              ECG: RS <input data-field="ecgFrecuencia" value="80" inputmode="numeric" aria-label="Frecuencia cardíaca del ECG" disabled> lpm, intervalo PR de duración normal, QRS fino, sin alteraciones del ST ni otros signos de isquemia aguda.
            </span>
          </label>
          <label class="ecg-editor-label" id="ecgDescripcionLabel" hidden>
            <textarea class="ecg-custom-editor" data-field="ecgDescripcion" aria-label="Descripción del ECG" placeholder="ECG: describa ritmo, frecuencia, intervalos, QRS, ST y otros hallazgos" disabled></textarea>
          </label>
        </div>
      </section>

      <section>
        <h2>Paraclínica e interconsultas</h2>
        <label>Estudios, resultados e interconsultas
          <textarea data-field="paraclinica" placeholder="Ej: RxTx, laboratorio, consulta con especialista, procedimientos, etc."></textarea>
        </label>
      </section>

      <section>
        <h2>Conducta</h2>
        <label>Conducta
          <textarea data-field="conducta" placeholder="Paraclínica, tratamiento, interconsultas, observación, alta, ingreso, etc."></textarea>
        </label>
        <div class="check-row">
          <label class="check"><input type="checkbox" data-field="reconsultaAlta"> Se explican signos de reconsulta/alta</label>
        </div>
      </section>
    </form>

    <aside class="column preview-column">
      <div class="preview-top">
        <strong>Texto generado</strong>
        <span class="status" id="status">Listo para copiar</span>
      </div>
      <pre id="historia"></pre>
    </aside>
  </main>

  <div class="suggestion-strip">
    <a class="suggestion-link" href="contacto.html#contacto">¿Tenés alguna sugerencia?</a>
  </div>

  <footer>
    Desarrollado por Dr. Santiago Saralegui · <a href="contacto.html">Contacto profesional</a>
  </footer>

  <script>
    const form = document.getElementById("hcForm");
    const preview = document.getElementById("historia");
    const status = document.getElementById("status");
    const antecedentesBox = document.getElementById("antecedentes");
    const alergiasInput = document.querySelector("[data-field='alergias']");
    const sinAlergias = document.querySelector("[data-field='sinAlergias']");
    const incluirEcg = document.querySelector("[data-field='incluirEcg']");
    const ecgNormal = document.querySelector("[data-field='ecgNormal']");
    const ecgOptions = document.getElementById("ecgOptions");
    const ecgNormalEditorLabel = document.getElementById("ecgNormalEditorLabel");
    const ecgDescripcionLabel = document.getElementById("ecgDescripcionLabel");

    function ecgNormalDescripcion() {
      const frecuencia = value("ecgFrecuencia") || "80";
      return `RS ${frecuencia} lpm, intervalo PR de duración normal, QRS fino, sin alteraciones del ST ni otros signos de isquemia aguda`;
    }

    function value(name) {
      const el = document.querySelector(`[data-field="${name}"]`);
      if (!el) return "";
      if (el.type === "checkbox" || el.type === "radio") return el.checked;
      return el.value.trim();
    }

    function compact(items) {
      return items.filter(Boolean).join(". ");
    }

    function sentence(label, text) {
      if (!text) return "";
      return `${label}: ${text}${/[.!?]$/.test(text) ? "" : "."}`;
    }

    function line(label, parts) {
      const text = parts.filter(Boolean).join(", ");
      return sentence(label, text);
    }

    function edadTexto(text) {
      if (!text) return "";
      return /\baños?\b/i.test(text) ? text : `${text} años`;
    }

    function fieldOrNormal(fieldName, checkboxName, normalText) {
      return value(fieldName) || (value(checkboxName) ? normalText : "");
    }

    function frTexto() {
      const raw = value("fr");
      if (!raw) return "eupneico";
      const normalized = raw.replace(",", ".");
      const match = normalized.match(/\d+(?:\.\d+)?/);
      const number = match ? Number(match[0]) : NaN;
      if (Number.isFinite(number) && number >= 20) return `polipnea de ${raw} rpm`;
      return `FR ${raw} rpm`;
    }

    function enfermedadActualTexto() {
      const items = [];
      if (value("enfermedad")) items.push(value("enfermedad"));
      if (value("cardioNegativo")) items.push("No dolor torácico, no palpitaciones, no síncope, no mareos");
      if (value("respNegativo")) items.push("No disnea, no dolor tipo puntada de lado");
      if (value("noFiebreChuchos")) items.push("No fiebre, no chuchos de frío");
      if (value("transitoNormal")) items.push("Tránsito sin alteraciones");
      return items.map((item) => `${item}${/[.!?]$/.test(item) ? "" : "."}`).join(" ");
    }

    function conductaTexto() {
      const items = [];
      const text = value("conducta");
      if (text) {
        items.push(...text.split(/\n+/).map((item) => item.trim()).filter(Boolean));
      }
      if (value("reconsultaAlta")) {
        items.push("Se explican signos de reconsulta");
        items.push("Alta");
      }
      if (!items.length) return "";
      return `Conducta:\n${items.map((item) => `- ${item}${/[.!?]$/.test(item) ? "" : "."}`).join("\n")}`;
    }

    function paraclinicaTexto() {
      const text = value("paraclinica");
      if (!text) return "";
      const items = text.split(/\n+/).map((item) => item.trim()).filter(Boolean);
      return items.map((item) => `${item}${/[.!?]$/.test(item) ? "" : "."}`).join("\n");
    }

    function ecgTexto() {
      if (!value("incluirEcg")) return "";
      if (!value("ecgNormal")) return sentence("ECG", value("ecgDescripcion"));
      return sentence("ECG", ecgNormalDescripcion());
    }

    function addAntecedente(data = {}) {
      const row = document.createElement("div");
      row.className = "antecedente-row";
      row.innerHTML = `
        <label>Antecedente
          <input class="ap-name" name="ap-name-${Date.now()}-${antecedentesBox.children.length}" autocomplete="off" placeholder="Ej: DM2, HTA, FA" value="${escapeAttr(data.name || "")}">
        </label>
        <label>Tratamiento
          <input class="ap-treatment" name="ap-treatment-${Date.now()}-${antecedentesBox.children.length}" autocomplete="off" placeholder="Ej: metformina, losartán" value="${escapeAttr(data.treatment || "")}">
        </label>
        <label>Otros
          <input class="ap-other" name="ap-other-${Date.now()}-${antecedentesBox.children.length}" autocomplete="off" placeholder="Ej: repercusiones crónicas" value="${escapeAttr(data.other || "")}">
        </label>
        <button class="mini-btn" type="button" title="Quitar antecedente">×</button>
      `;

      row.querySelector(".mini-btn").addEventListener("click", () => {
        row.remove();
        if (!antecedentesBox.children.length) addAntecedente();
        update();
      });

      row.querySelectorAll("input").forEach((input) => {
        input.addEventListener("input", update);
        input.addEventListener("keydown", (event) => {
          if (event.key === "Enter") {
            event.preventDefault();
            const next = addAntecedente();
            next.querySelector(".ap-name").focus();
          }
        });
      });

      antecedentesBox.appendChild(row);
      return row;
    }

    function escapeAttr(text) {
      return String(text).replaceAll("&", "&amp;").replaceAll('"', "&quot;").replaceAll("<", "&lt;");
    }

    function antecedentesItems() {
      const rows = [...antecedentesBox.querySelectorAll(".antecedente-row")];
      return rows.map((row) => {
        const name = row.querySelector(".ap-name").value.trim();
        const treatment = row.querySelector(".ap-treatment").value.trim();
        const other = row.querySelector(".ap-other").value.trim();
        if (!name) return "";
        const details = [];
        if (treatment) details.push(`en tratamiento con ${treatment}`);
        if (other) details.push(other);
        return details.length ? `${name} ${details.join(", ")}` : name;
      }).filter(Boolean);
    }

    function antecedentesTexto(extraItems = []) {
      const apItems = antecedentesItems();
      const items = [...(apItems.length ? apItems : ["Sin antecedentes a destacar"]), ...extraItems];
      if (!items.length) return "";
      return `AP:\n${items.map((item) => `- ${item}${/[.!?]$/.test(item) ? "" : "."}`).join("\n")}`;
    }

    function buildHistoria() {
      const fichaParts = [];
      if (value("sexoF")) fichaParts.push("SF");
      if (value("sexoM")) fichaParts.push("SM");
      if (value("edad")) fichaParts.push(edadTexto(value("edad")));
      if (value("trabajo")) fichaParts.push(value("trabajo"));
      if (value("procedencia")) fichaParts.push(`procedente de ${value("procedencia")}`);
      if (value("acompanante")) fichaParts.push(`acompañado por ${value("acompanante")}`);
      if (value("autovalido")) fichaParts.push("autoválido");
      if (value("dependiente")) fichaParts.push("dependiente");
      if (value("institucionalizado")) fichaParts.push(value("sexoF") ? "institucionalizada" : "institucionalizado");

      const ficha = fichaParts.length ? `FP: ${fichaParts.join(", ")}.` : "";

      const alergiaItems = [];
      if (value("sinAlergias")) {
        alergiaItems.push("Sin alergias medicamentosas conocidas");
      } else if (value("alergias")) {
        alergiaItems.push(`Alergias: ${value("alergias")}`);
      }

      const antecedentes = antecedentesTexto(alergiaItems);

      const generalParts = [value("general")];
      if (value("hgt")) generalParts.push(`HGT ${value("hgt")}`);

      const pymParts = [value("pym")];
      if (value("temp")) pymParts.push(`Tax ${value("temp")}`);

      const cvParts = [];
      if (value("pa")) cvParts.push(`PA ${value("pa")} mmHg`);
      if (value("fc")) cvParts.push(`FC ${value("fc")} cpm`);
      if (value("cv")) cvParts.push(value("cv"));

      const ppParts = [];
      ppParts.push(frTexto());
      if (value("sat")) ppParts.push(`SatO2 ${value("sat")}`);
      if (value("pp")) ppParts.push(value("pp"));

      const examen = [
        generalParts.filter(Boolean).join(", ") ? `${generalParts.filter(Boolean).join(", ")}.` : "",
        line("PyM", pymParts),
        line("CV", cvParts),
        line("PP", ppParts),
        sentence("MMII", value("mmii")),
        sentence("ABD", fieldOrNormal("abd", "abdNormal", "Blando, depresible, indoloro, RHA +")),
        sentence("LG", fieldOrNormal("lg", "lgNormal", "Sin adenomegalias")),
        sentence("OA", value("oa")),
        sentence("PNM", fieldOrNormal("pnm", "pnmNormal", "Glasgow 15, pupilas reactivas y simétricas, sin síndrome focal neurológico")),
        sentence("FFLL", fieldOrNormal("ffll", "ffllNormal", "Libres e indoloras")),
        value("otrosEf") ? `${value("otrosEf")}${/[.!?]$/.test(value("otrosEf")) ? "" : "."}` : ""
      ].filter(Boolean).join("\n");

      const blocks = [
        ficha,
        antecedentes,
        sentence("EA", enfermedadActualTexto()),
        examen ? `Examen físico:\n${examen}` : "",
        ecgTexto(),
        paraclinicaTexto(),
        conductaTexto()
      ].filter(Boolean);

      return blocks.join("\n\n");
    }

    function update() {
      preview.textContent = buildHistoria() || "Complete campos a la izquierda para generar la historia clínica.";
      const disabled = sinAlergias.checked;
      alergiasInput.disabled = disabled;
      if (disabled) alergiasInput.value = "";
      ecgOptions.classList.toggle("is-disabled", !incluirEcg.checked);
      ecgNormal.disabled = !incluirEcg.checked;
      document.querySelector("[data-field='ecgFrecuencia']").disabled = !incluirEcg.checked || !ecgNormal.checked;
      document.querySelector("[data-field='ecgDescripcion']").disabled = !incluirEcg.checked || ecgNormal.checked;
      ecgNormalEditorLabel.hidden = !ecgNormal.checked;
      ecgDescripcionLabel.hidden = ecgNormal.checked;
      status.textContent = "Actualizado";
      status.className = "status";
    }

    async function copyHistoria() {
      const text = buildHistoria();
      if (!text) return;
      try {
        await navigator.clipboard.writeText(text);
      } catch {
        const temp = document.createElement("textarea");
        temp.value = text;
        document.body.appendChild(temp);
        temp.select();
        document.execCommand("copy");
        temp.remove();
      }
      status.textContent = "Copiado al portapapeles";
      status.className = "status toast";
    }

    function downloadTxt() {
      const text = buildHistoria();
      if (!text) return;
      const blob = new Blob([text], { type: "text/plain;charset=utf-8" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "historia-clinica-emergencia.txt";
      a.click();
      URL.revokeObjectURL(url);
    }

    function clearAll() {
      if (!confirm("¿Limpiar todos los campos?")) return;
      form.reset();
      antecedentesBox.innerHTML = "";
      addAntecedente();
      document.querySelector("[data-field='funcionalidadNoConsignar']").checked = true;
      document.querySelector("[data-field='sinAlergias']").checked = true;
      document.querySelector("[data-field='noFiebreChuchos']").checked = true;
      document.querySelector("[data-field='transitoNormal']").checked = true;
      document.querySelector("[data-field='fc']").value = "80";
      document.querySelector("[data-field='general']").value = "Lúcido";
      document.querySelector("[data-field='pym']").value = "BHyP";
      document.querySelector("[data-field='cv']").value = "RR, RBG, SL";
      document.querySelector("[data-field='pp']").value = "MAV + bilateral, sin estertores";
      document.querySelector("[data-field='ecgFrecuencia']").value = "80";
      ecgNormal.checked = true;
      update();
    }

    let expandedEditor = null;

    function closeExpandedEditor() {
      if (!expandedEditor) return;
      expandedEditor.remove();
      expandedEditor = null;
    }

    function openExpandedEditor(input) {
      if (expandedEditor?.source === input) return;
      closeExpandedEditor();
      const editor = document.createElement("textarea");
      const rect = input.getBoundingClientRect();
      editor.className = "expanded-editor";
      editor.value = input.value;
      editor.placeholder = input.placeholder;
      editor.source = input;
      const editorWidth = Math.min(520, window.innerWidth - 24);
      editor.style.left = `${Math.max(12, Math.min(rect.left, window.innerWidth - editorWidth - 12))}px`;
      editor.style.top = `${Math.min(rect.bottom + 6, window.innerHeight - 132)}px`;
      editor.addEventListener("input", () => {
        input.value = editor.value.replace(/\n+/g, " ");
        input.dispatchEvent(new Event("input", { bubbles: true }));
      });
      editor.addEventListener("blur", closeExpandedEditor);
      document.body.appendChild(editor);
      expandedEditor = editor;
      editor.focus();
      editor.setSelectionRange(editor.value.length, editor.value.length);
    }

    form.addEventListener("focusin", (event) => {
      const input = event.target;
      if (expandedEditor && input !== expandedEditor && input !== expandedEditor.source) closeExpandedEditor();
      if (input.matches("input:not([type='checkbox']):not([type='radio'])") && input.scrollWidth > input.clientWidth) {
        openExpandedEditor(input);
      }
    });

    form.addEventListener("input", (event) => {
      const input = event.target;
      if (input.matches("input:not([type='checkbox']):not([type='radio'])") && input.scrollWidth > input.clientWidth) {
        openExpandedEditor(input);
      }
    });

    incluirEcg.addEventListener("change", () => {
      if (incluirEcg.checked) ecgNormal.checked = true;
      update();
    });

    ecgNormal.addEventListener("change", () => {
      if (!ecgNormal.checked && !value("ecgDescripcion")) {
        document.querySelector("[data-field='ecgDescripcion']").value = ecgNormalDescripcion();
      }
      update();
    });

    document.getElementById("copyTop").addEventListener("click", copyHistoria);
    document.getElementById("downloadTxt").addEventListener("click", downloadTxt);
    document.getElementById("clearAll").addEventListener("click", clearAll);
    document.getElementById("addAntecedente").addEventListener("click", () => {
      const row = addAntecedente();
      row.querySelector(".ap-name").focus();
      update();
    });

    form.addEventListener("input", update);
    form.addEventListener("change", update);

    addAntecedente();
    update();
  </script>
</body>
</html>
